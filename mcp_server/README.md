# justtwod MCP server

The Python MCP server that drives Adobe Photoshop.

See the [repo README](../README.md) for overview, install, and config.

## Run standalone (for debugging)

```bash
uv run justtwod
```

Speaks MCP over stdio. Point any MCP client at this command.
